{
  "name": "@dare/sdk",
  "version": "0.1.0",
  "description": "Node.js/TypeScript SDK for the dare LLM debugging CLI — captures errors and forwards them to the core binary for diagnosis.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test dist/**/*.test.js"
  },
  "dependencies": {
    "which": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/which": "^3.0.0",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
